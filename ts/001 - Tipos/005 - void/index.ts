/* =====================
    Tipos no TypeScript
   ===================== */

const writtingMessage = (message: string): void => {
  console.log(message);
};

writtingMessage('Olá, eu sou um exemplo de mensagem! :)');
